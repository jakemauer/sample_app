<% provide(:title, "All users") %>
<h1>All users</h1>

<%= will_paginate %>

<ul class="users">
  <%= render @users %>
  <%# --- Alternative longer syntax that doesn't use Rails' crazy-deep conventions  %>
  <%# @users.each do |user| %>
    <%#= render 'user', user: user %>
  <%# end %>
</ul>

<%= will_paginate %>